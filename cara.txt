List every controller action in src/AppPortal.API/Controllers/, one line per action, in this exact format:

ControllerName | HTTP verb | Route | Current [Authorize] attribute (if any)

Group by controller. Include every action, even ones with no explicit route attribute. Just paste the plain list — don't summarize, don't add commentary, don't skip anything.
